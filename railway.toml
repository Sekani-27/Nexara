[build]
builder = "dockerfile"

[deploy]
startCommand = "python run_multi.py"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30

[deploy.resources]
memory = "512Mi"

[deploy.mounts]
mountPath = "/app/data"
